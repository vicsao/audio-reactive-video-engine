# ==============================================
# Video Generator Configuration File (v12.0)
# VinCreationz International Edition
# ==============================================

# --- RENDER SETTINGS ---
# True = Generates 20s previews (fast). 
# False = Generates full length videos (MASTER build).
TEST_MODE=True

# NEW v12.5 Toggle: True = Process video. 
# False = Skip video, generate only Art/LRC.
RENDER_VIDEO=True

# True = Uses randomly cropped/panned images with blurred backgrounds.
# False = Uses static, full-screen images.
RANDOM_CROP_IMAGES=False

# --- BRANDING ---
# The name that appears below the main song title.
ARTIST_NAME=VinCreationz

# The text that appears in the bottom right corner throughout the video.
WATERMARK_TEXT=VinCreationz

# --- LOCALIZATION & FONTS (Updated v12.0) ---
# Path to the font file that supports Chinese (Unicode) characters.
# The script uses this for Mandarin lyrics and dual-language cover art.
# v12.0 Auto-Detect: Script will default to 'Arial-Bold' if no Chinese is found.
CHINESE_FONT_PATH=msyhbd.ttc