# ==============================================
# Video Generator Configuration File (v13.1)
# VinCreationz Production Suite
# ==============================================

# --- RENDER SETTINGS ---
# True = Generates 20s previews (fast testing). 
# False = Generates full length videos (MASTER build).
TEST_MODE=False

# True = Process video (MP4). 
# False = Skip video, generate only Art/LRC/Thumbnails (Dev Mode).
RENDER_VIDEO=True

# --- FUTURE MODULES (DISABLED) ---
# GENERATE_SHORT=False
# ADD_INTRO_SILENCE=False

# --- VISUAL STYLE ---
# True = Uses Clean Border Engine (blurred bg + 2px white border).
# False = Uses static, full-screen image assets.
RANDOM_CROP_IMAGES=True

# --- BRANDING ---
# Main artist identifier appearing below song titles on thumbnails and art.
ARTIST_NAME=VinCreationz

# Watermark for the bottom-right corner of the video Master.
WATERMARK_TEXT=VinCreationz

# --- LOCALIZATION & FONTS ---
# Path to the font file supporting Unicode/Mandarin characters.
# REQUIRED for Suno Safe-Zone Art & Multi-language lyric stacking.
CHINESE_FONT_PATH=msyhbd.ttc